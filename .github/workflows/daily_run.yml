name: Daily News Analysis & Governance Bot

on:
  schedule:
    # 1. 한국 시간 오전 06:00 (KST) = UTC 21:00 전날 - 산업군 모니터링
    - cron: '0 21 * * *'
    # 2. 한국 시간 오전 09:00 (KST) = UTC 00:00 - 정기 브리핑 및 데이터 동기화
    - cron: '0 0 * * *'
    # 3. 한국 시간 밤 23:30 (KST) = UTC 14:30 - 지능 자동 승인 및 마감 작업
    - cron: '30 14 * * *'
  workflow_dispatch:
    inputs:
      backlog_id:
        description: '처리할 개발 백로그 ID (HQ 연동)'
        required: false
        default: ''
      force_cron_type:
        description: '강제 실행 타입 (BRIEFING / INDUSTRY / GOVERNANCE)'
        required: false
        default: ''

concurrency:
  group: daily-news-bot-${{ github.ref }}
  cancel-in-progress: false

jobs:
  execute-bot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          ref: ${{ github.ref_name }}

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install supabase google-genai gnews resend

      - name: Install Playwright browser (Chromium)
        run: |
          python -m playwright install --with-deps chromium

      - name: E2E smoke gate (login/onboarding/keyword)
        run: |
          python scripts/e2e_smoke_gate.py

      # CRON_TYPE 결정: 스케줄별로 명확히 분리
      - name: Determine CRON_TYPE
        id: cron_type
        run: |
          SCHEDULE="${{ github.event.schedule }}"
          FORCE="${{ github.event.inputs.force_cron_type }}"

          if [ -n "$FORCE" ]; then
            echo "CRON_TYPE=$FORCE" >> $GITHUB_ENV
            echo "✅ 강제 실행 타입: $FORCE"
          elif [ "$SCHEDULE" = "30 14 * * *" ]; then
            echo "CRON_TYPE=GOVERNANCE" >> $GITHUB_ENV
            echo "✅ 스케줄 감지: GOVERNANCE (23:30 KST)"
          elif [ "$SCHEDULE" = "0 21 * * *" ]; then
            echo "CRON_TYPE=INDUSTRY" >> $GITHUB_ENV
            echo "✅ 스케줄 감지: INDUSTRY (06:00 KST)"
          elif [ "$SCHEDULE" = "0 0 * * *" ]; then
            echo "CRON_TYPE=BRIEFING" >> $GITHUB_ENV
            echo "✅ 스케줄 감지: BRIEFING (09:00 KST)"
          else
            echo "CRON_TYPE=BRIEFING" >> $GITHUB_ENV
            echo "✅ 수동 실행 기본값: BRIEFING"
          fi

      - name: Record release ledger (start)
        if: always()
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          if [ -z "$SUPABASE_URL" ] || [ -z "$SUPABASE_SERVICE_ROLE_KEY" ]; then
            echo "release_ledger skipped: missing secrets"
            exit 0
          fi
          TS=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          PAYLOAD=$(cat <<JSON
          [{
            "run_id":"${GITHUB_RUN_ID}",
            "release_type":"BOT_EXECUTION",
            "trigger_type":"${GITHUB_EVENT_NAME}",
            "cron_type":"${CRON_TYPE}",
            "branch":"${GITHUB_REF_NAME:-main}",
            "commit_sha":"${GITHUB_SHA}",
            "backlog_id":"${{ github.event.inputs.backlog_id }}",
            "status":"RUNNING",
            "note":"workflow started",
            "released_at":"${TS}"
          }]
          JSON
          )
          curl -sS -X POST "${SUPABASE_URL}/rest/v1/release_ledger?on_conflict=run_id" \
          -H "apikey: ${SUPABASE_SERVICE_ROLE_KEY}" \
          -H "Authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}" \
          -H "Content-Type: application/json" \
          -H "Prefer: resolution=merge-duplicates,return=minimal" \
          -d "${PAYLOAD}" || echo "release_ledger start write failed (non-blocking)"

      - name: Run news_bot.py
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          CURRENT_BACKLOG_ID: ${{ github.event.inputs.backlog_id }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          CRON_TYPE: ${{ env.CRON_TYPE }}
        run: |
          echo "🚀 실행 타입: $CRON_TYPE"
          python news_bot.py

      - name: Final Data Sync (safe)
        run: |
          git config --global user.name "Fitz Intelligence Bot"
          git config --global user.email "positivecha@gmail.com"
          if [ -f data.json ]; then
            git add data.json
          fi
          if ! git diff --cached --quiet -- data.json; then
            git commit -m "📊 [Workflow Sync] $(date +'%Y-%m-%d %H:%M') KST - ${{ env.CRON_TYPE }}"
            git push origin HEAD:${GITHUB_REF_NAME:-main}
          else
            echo "No data.json changes detected to push."
          fi

      - name: Record release ledger (success)
        if: success()
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          if [ -z "$SUPABASE_URL" ] || [ -z "$SUPABASE_SERVICE_ROLE_KEY" ]; then
            echo "release_ledger success skipped: missing secrets"
            exit 0
          fi
          TS=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          PAYLOAD=$(cat <<JSON
          {
            "status":"SUCCEEDED",
            "completed_at":"${TS}",
            "note":"workflow completed successfully"
          }
          JSON
          )
          curl -sS -X PATCH "${SUPABASE_URL}/rest/v1/release_ledger?run_id=eq.${GITHUB_RUN_ID}" \
          -H "apikey: ${SUPABASE_SERVICE_ROLE_KEY}" \
          -H "Authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}" \
          -H "Content-Type: application/json" \
          -d "${PAYLOAD}" || echo "release_ledger success write failed (non-blocking)"

      - name: Record release ledger (failure)
        if: failure()
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          if [ -z "$SUPABASE_URL" ] || [ -z "$SUPABASE_SERVICE_ROLE_KEY" ]; then
            echo "release_ledger failure skipped: missing secrets"
            exit 0
          fi
          TS=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          RUN_URL="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          PAYLOAD=$(cat <<JSON
          {
            "status":"FAILED",
            "completed_at":"${TS}",
            "note":"workflow failed: ${RUN_URL}"
          }
          JSON
          )
          curl -sS -X PATCH "${SUPABASE_URL}/rest/v1/release_ledger?run_id=eq.${GITHUB_RUN_ID}" \
          -H "apikey: ${SUPABASE_SERVICE_ROLE_KEY}" \
          -H "Authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}" \
          -H "Content-Type: application/json" \
          -d "${PAYLOAD}" || echo "release_ledger failure write failed (non-blocking)"

      - name: Notify Sovereign HQ (Success)
        if: success() && github.event.inputs.backlog_id != ''
        run: |
          curl -X PATCH "${{ secrets.SUPABASE_URL }}/rest/v1/dev_backlog?id=eq.${{ github.event.inputs.backlog_id }}" \
          -H "apikey: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
          -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
          -H "Content-Type: application/json" \
          -d '{"status": "DEPLOYED", "completed_at": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"}'

          curl -X POST "${{ secrets.SUPABASE_URL }}/rest/v1/action_logs" \
          -H "apikey: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
          -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
          -H "Content-Type: application/json" \
          -d '{"action_type": "DEV_DEPLOY", "target_word": "SYSTEM", "details": "✅ AI 배포 완료: 백로그 #${{ github.event.inputs.backlog_id }}가 성공적으로 반영되었습니다."}'

      - name: Notify Sovereign HQ (Failure)
        if: failure() && github.event.inputs.backlog_id != ''
        run: |
          curl -X PATCH "${{ secrets.SUPABASE_URL }}/rest/v1/dev_backlog?id=eq.${{ github.event.inputs.backlog_id }}" \
          -H "apikey: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
          -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
          -H "Content-Type: application/json" \
          -d '{"status": "FAILED"}'

          curl -X POST "${{ secrets.SUPABASE_URL }}/rest/v1/action_logs" \
          -H "apikey: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
          -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
          -H "Content-Type: application/json" \
          -d '{"action_type": "DEV_DEPLOY", "target_word": "SYSTEM", "details": "❌ AI 배포 실패: 백로그 #${{ github.event.inputs.backlog_id }} 실행 중 오류 발생"}'
